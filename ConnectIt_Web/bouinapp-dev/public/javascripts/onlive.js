//

var day = DAY;


var location = {
  datetime: type: Date,
  longitude: Number,
  latitude: Number,
  altitude : Number,
  speed : Number,
  status: String
};

// http://localhost:3000/api/locations/daily/day2

$.get("http://localhost:3000/api/locations/daily/"+DAY){
	[{location}, {location}];
}

